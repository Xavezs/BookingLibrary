{
  "name": "bookworm",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack Library Book Borrowing & Reservation web application",
  "scripts": {
    "install:all": "npm install --workspaces --include-workspace-root",
    "dev": "npm run build --workspace client && npm run dev --workspace server",
    "start": "npm run build --workspace client && npm run start --workspace server",
    "build": "npm run build --workspace client && node server/scripts/copy-client-dist.mjs",
    "lint": "npm run lint --workspace client"
  },
  "workspaces": [
    "server",
    "client"
  ],
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
